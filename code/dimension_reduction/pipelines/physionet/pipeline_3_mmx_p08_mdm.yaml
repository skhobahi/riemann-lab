imports:
    pyriemann.estimation:
        - Covariances
    pyriemann.classification:
        - MDM
    utilities.dim_reduction:
        - RDR

pipeline:
    - Covariances:
        estimator: 'oas'
    - RDR:
        n_components: 8
        method: 'minmax'
        alpha: 1.0
    - MDM:
        metric: 'riemann'

label: 'minmax (8) + mdm'